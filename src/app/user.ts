export class User {
    static result: any;
    constructor( public avater_url: any, public name:string, public login:string, public bio:string, public avatar_url:string,public html_url:any, public public_repos:number,public following:number, public followers:number, public created_at:Date){
  
    }
  }